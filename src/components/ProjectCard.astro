---
import type { Project } from '../data/content'

interface Props {
  project: Project
  opensInNewTab: string
  repoLabel: string
  featured?: boolean
}

const { project, opensInNewTab, repoLabel, featured = false } = Astro.props
---

<article class:list={['project-card', 'card', { featured }]}>
  <img class="cover" src={project.image} alt="" width="760" height="460" loading="lazy" />

  <div class="body">
    <h3>{project.name}</h3>
    <p class="role">{project.role}</p>
    <p class="description">{project.description}</p>

    <ul class="tags">
      {project.tech.map((tech) => <li class="tag">{tech}</li>)}
    </ul>

    <div class="links">
      <a href={project.url} target="_blank" rel="noopener noreferrer">
        {project.name} <span aria-hidden="true">↗</span>
        <span class="visually-hidden">({opensInNewTab})</span>
      </a>

      {
        project.repoUrl && (
          <a class="repo-link" href={project.repoUrl} target="_blank" rel="noopener noreferrer">
            {repoLabel} <span aria-hidden="true">↗</span>
            <span class="visually-hidden">({opensInNewTab})</span>
          </a>
        )
      }
    </div>
  </div>
</article>

<style>
  .project-card {
    display: flex;
    flex-direction: column;
    padding: 0;
    overflow: hidden;
  }

  .cover {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 76 / 46;
    object-fit: cover;
    object-position: top;
    border-block-end: 1px solid var(--border);
  }

  .body {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    padding: 1.5rem;
    flex-grow: 1;
  }

  h3 {
    margin: 0;
    font-size: 1.15rem;
  }

  .role {
    margin: 0;
    font-size: 0.86rem;
    font-weight: 600;
    color: #c9b8f5;
  }

  .description {
    margin: 0;
    color: var(--text-muted);
    font-size: 0.95rem;
    flex-grow: 1;
  }

  .links {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    margin-block-start: 0.4rem;
  }

  a {
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
    color: var(--text);
  }

  a:hover {
    text-decoration: underline;
  }

  .repo-link {
    margin-inline-start: 0.9rem;
    padding-inline-start: 0.9rem;
    border-inline-start: 1px solid var(--border);
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--text-muted);
  }

  .repo-link:hover {
    color: var(--text);
  }

  /* Wide featured card: the lead project runs full width with the cover
     beside the text instead of above it, using the extra width. */
  @media (min-width: 700px) {
    .project-card.featured {
      flex-direction: row;
    }

    .project-card.featured .cover {
      width: 54%;
      height: auto;
      aspect-ratio: auto;
      align-self: stretch;
      border-block-end: none;
      border-inline-end: 1px solid var(--border);
    }

    .project-card.featured .body {
      width: 46%;
      justify-content: center;
    }

    .project-card.featured h3 {
      font-size: 1.4rem;
    }
  }
</style>
